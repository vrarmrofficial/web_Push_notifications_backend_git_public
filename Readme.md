INSTALL BACKEND:
 - npm init -y

 - npm install express firebase-admin cors body-parser node-cron


 - node server.js


 Note: if we pull it from git, just need to install theh dependecies using "npm install"